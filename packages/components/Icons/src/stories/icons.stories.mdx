import { Meta, Source } from '@storybook/blocks'
import pkg from '../../package.json'

<Meta title="Componentes/Icons/Versão" />

## Versão do pacote individual

<Source code={`${pkg.name}: ${pkg.version}`} dark={true} />